__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
_tmp_*/
*.log
_log_ctp.txt
# o nome EXATO nao basta: em 17/09/2026 um 'config_local.py.ANTES-17-09-2026'
# - copia feita antes de trocar o DSN - passou direto e foi parar no
# repositorio, que e PUBLICO. Naquele nao havia senha, so caminhos e o
# numero do funcionario. Mas o proximo pode ter, e ninguem vai lembrar
# de conferir na hora. O curinga fecha a porta para toda a familia.
config_local.py*
legacy/

# ajustes do Claude Code desta maquina (caminhos locais)
.claude/settings.local.json

# rascunho do to-tickets (nao e trabalho, e recado para mim mesmo)
.scratch/

# O MAPA DO CODIGO NAO SE VERSIONA. Decidido em 20/09/2026.
#
# Ele descreve o que ja se le no codigo, e envelhece calado: o de 18/09
# tinha DOIS DIAS e ja nao sabia do paginacao.py, do leitor do Preps nem
# da bancada - e ninguem percebeu, porque mapa errado nao da erro em
# lugar nenhum. E a mesma doenca que a folha escrita em dois lugares.
#
# A skill cartographer continua aqui (.claude/skills/cartographer): gere
# o mapa quando precisar se orientar, leia, e jogue fora. O que NAO se le
# no codigo - por que a regra existe, o caso, o numero medido - mora nas
# skills e nos comentarios, e essa metade vai em git.
docs/CODEBASE_MAP.md

# arte de cliente usada em teste - nao e codigo, e nao vai para o GitHub
ARQUIVOS TEMP PARA TESTES/
